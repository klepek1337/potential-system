[project]
name = "okx-ma-telegram-alerts"
version = "1.6.0"
description = "OKX H4 moving-average tests sent to Telegram"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
